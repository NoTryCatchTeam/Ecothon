@inject NavigationManager NavigationManager

@code {

    protected override void OnInitialized()
        => NavigationManager.NavigateTo($"/login?redirect-uri={Uri.EscapeDataString(NavigationManager.BaseUri)}", true);

}